[workspace]
resolver = "2"
members = [
    "qchess-bot",
    "train-against-stockfish",
    "train-against-self",
]

# For training on kaggle environment
[profile.kaggle]
inherits = "release"
lto = false
